apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vault
  namespace: ns-monitoring
  labels:
    release: prometheus-grafana
spec:
  selector:
    matchLabels:
      app: vault
  namespaceSelector:
    matchNames:
      - ns-vault
  endpoints:
    - port: telemetry
      path: /metrics
      interval: 30s
