apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pvc-new
  namespace: ns-devops
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-fs
