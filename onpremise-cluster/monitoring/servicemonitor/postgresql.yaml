apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: postgresql
  namespace: ns-monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: postgresql
  namespaceSelector:
    matchNames:
      - ns-app
  endpoints:
    - port: metrics
      interval: 30s
