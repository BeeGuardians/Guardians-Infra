apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vault-exporter
  namespace: ns-monitoring
  labels:
    release: prometheus-grafana
spec:
  selector:
    matchLabels:
      app: vault-exporter
  namespaceSelector:
    matchNames:
      - ns-monitoring
  endpoints:
    - port: http
      path: /metrics
      interval: 30s