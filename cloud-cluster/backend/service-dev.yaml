apiVersion: v1
kind: Service
metadata:
  name: backend-dev
  namespace: ns-app
  labels:
    app: backend-dev
spec:
  type: NodePort
  selector:
    app: backend-dev
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30090
