apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: harbor
  namespace: ns-monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: harbor
  namespaceSelector:
    matchNames:
      - ns-app
  endpoints:
    - port: metrics
      interval: 30s
