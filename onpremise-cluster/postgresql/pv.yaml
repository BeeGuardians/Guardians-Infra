apiVersion: v1
kind: PersistentVolume
metadata:
  name: pgdata-refresh-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/postgres
