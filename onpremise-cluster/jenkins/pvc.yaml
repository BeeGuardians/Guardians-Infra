apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-refresh
  namespace: ns-devops
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-fs
