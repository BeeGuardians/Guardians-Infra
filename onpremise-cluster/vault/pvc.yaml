apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-data
  namespace: ns-vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-block
