apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-mgr-dashboard
  namespace: rook-ceph
  labels:
    app: rook-ceph-mgr
spec:
  type: NodePort
  selector:
    app: rook-ceph-mgr
  ports:
    - name: dashboard
      port: 7000
      targetPort: 7000
      nodePort: 30201
      protocol: TCP
